login_host = "0.0.0.0"
login_port = 9000

supernode_host = "0.0.0.0"
supernode_port = 9001

relay_host = "0.0.0.0"
relay_port = 9002

supernode_cache_size = 128
peer_cluster_limit = 500
database_url = "sqlite://./data/skype-rs.db"

# Optional bootstrap list for supernode-to-supernode directory federation.
known_supernodes = ["127.0.0.1:9001"]
